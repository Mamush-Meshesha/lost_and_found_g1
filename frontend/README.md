# Frontend

React frontend for the hackathon project.

## Getting Started

```bash
npm install
npm start
```

The app runs at `http://localhost:3000`.

## Available Scripts

- `npm start` - Start the development server
- `npm run build` - Create a production build
- `npm test` - Run tests
